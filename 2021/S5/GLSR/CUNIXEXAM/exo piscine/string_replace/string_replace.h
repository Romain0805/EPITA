#ifndef STRING_REPLACE
#define STRING_REPLACE

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

char *string_replace(const char *old_path, const char *str,
                     const char *new_pat);

#endif /* !STRING_REPLACE */